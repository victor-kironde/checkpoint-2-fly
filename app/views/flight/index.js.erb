$('#results').empty()
<% if @search.errors.any? %>
  <% @search.errors.each do |error| %>
    Materialize.toast('<%= error %>', 4000, 'rounded')
  <% end %>
<% else %>
  $('#results')
    .html("<%= j(render partial: 'partials/flight-results', locals: { search: @search } ) %>");
<% end %>
