$('#results').empty()
<% if @search.errors.any? %>
  <% @search.errors.each do |error| %>
    <%= j(flash[:danger] = error) %>
  <% end %>
<% else %>
  $('#results')
    .html("<%= j(render partial: 'partials/flight_results', locals: { search: @search } ) %>");
<% end %>
